export interface SlugProps {
  params: Promise<{ slug: any }>;
}
export interface OptionalProps {
  params: Promise<{ optional: any }>;
}
export interface DependsProps {
  params: Promise<{ slug: any }>;
}
export interface Slug2Props {
  params: Promise<{ slug2: any }>;
}